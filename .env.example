# Copy this file to .env and fill in real values for your machine.
# app/config.py reads every one of these via os.environ.get(), so
# nothing here is required — the app falls back to the defaults
# baked into Config if a variable is missing — but a real deployment
# should always override SECRET_KEY and the database password.
SECRET_KEY=pasalify-change-this-in-production-2026
MYSQL_HOST=localhost
MYSQL_USER=root
MYSQL_PASSWORD=admin@123
MYSQL_DB=sprint3
MYSQL_PORT=3306
