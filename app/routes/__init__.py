"""
app/routes package
================================================================
Intentionally empty — this file's only job is to mark the
'routes' folder as a Python package. Each blueprint is imported
directly from its own module (e.g. app.routes.auth) inside
create_app(), in app/app.py.
"""
